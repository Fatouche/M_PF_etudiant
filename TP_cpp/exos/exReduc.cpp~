#include <algorithm>
#include <functional>
#include <iostream>
#include <vector>


int main(){

  std::vector<int> liste {1,2,3};
  
  std::cout << std::accumulate(liste.begin(), liste.end(), 0, [] plus<int>); std::endl;


}
